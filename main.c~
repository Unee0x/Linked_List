#include "Node.h"
#include <stdlib.h>
#include <time.h>

int main( void ){
  srand(time(NULL));

  node_t head = NULL;

  for(int i = 0; i < 10;i++){
    int r = rand()%9+0;
    addNode(head, &r);
  }

  rmList(head);
  
  return 0;
}
